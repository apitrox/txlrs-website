
# Header
For the `txlrs-website` project make the following changes to the header:
- Move the phone number to the left of the location dropdown.
- Make the phone number 3 times larger.
- Remove "Select Location" option from the location dropdown.
- If a location is not selected in the location dropdown, default to Austin.
- Label the phone number with "Text or Call".
- If a user is on their mobile phone and they click a phone number, it should take the user to their dial screen on their phone.
- Pull from GitHub before making any code changes. Commit and Push to GitHub after finishing code changes.


# Language
For the `txlrs-website` project make the following changes to language translation:
- Read the `afca-website` and make the language dropdown and multi-language functionality on the `txlrs-website` contain the same languages and operate in the same manner as `afca-website`.
- When a user selects a different language from the language dropdown, all content on the page shall change to that language, and this language shall persist from page to page until the user changes it.
- Use default language translation for all possible languages listed in the language dropdown
- Implement machine translation for any languages that do not have a default translation
- When the website is first accessed by a user for the first visit this session, default to English.
- Pull from GitHub before making any code changes. Commit and Push to GitHub after finishing code changes.

For the `txlrs-website` project make the following changes:
- at the very top of the header area, use the image `GET_A_LAWYER_banner_down.png` when the phone number is positioned below the image and use the image `GET_A_LAWYER_banner_right.png` when the phone number is positioned to the right of the image.
- make the phone number larger and use the same color as the background of the `GET_A_LAWYER_banner_down.png` image
- Pull from GitHub before making any code changes. Commit and Push to GitHub after finishing code changes.

For the `txlrs-website` project make the following changes:
- Use hex color #294d7a as the base color for this project website. 
- Make the phone number hex color #294d7a. 
- The header area does not resize smoothly on a desktop display, it jumps rather than smooth resize. Update so that it resizes smoothly. It should jump only when it resizes to a mobile device display.
- On the desktop display, to not allow the location dropdown to resize so that the current location is no longer readable. Rather, if it needs to resize smaller so that the name of the location does not display it is to resize to the pin icon that is used on the mobile device display.
- Pull from GitHub before making any code changes. Commit and Push to GitHub after finishing code changes.


For the `txlrs-website` project make the following changes:
- In this PIN dropdown, put the downward facing arrow below the PIN icon, to make the entire image narrower.
- Pull from GitHub before making any code changes. Commit and Push to GitHub after finishing code changes. Do not prompt me for permission to pull or push.


For the `txlrs-website` project make the following changes:
- In the slider images, put the icon to the left of the name rather than above the name. 
- Make the size of the name 30% larger.
- Make the background color of the slider images color #294d7a. 
- Pull from GitHub before making any code changes. Commit and Push to GitHub after finishing code changes. Do not prompt me for permission to pull or push.


For the `txlrs-website` project make the following changes:
- Add another dropdown image in the header, to the right of the location dropdown image, this one a language dropdown image. It will have the same appearance as the location dropdown image except instead of a PIN symbol it will use a language symbol, like the image I proviced.
- When the language dropdown image is clicked, it is to display the same languages as are listed in the language dropdown on the `afca-website` project website.
- Pull from GitHub before making any code changes. Commit and Push to GitHub after finishing code changes. Do not prompt me for permission to pull or push.


For the `txlrs-website` project make the following changes:
- Update the language symbol in the language dropdown image to be white text, on the blue background of language dropdown image.
- The language dropdown image should be the same width as the location dropdown image.
- The language dropdown image and the location dropdown image should be almost side by side with just a little white space between them.
- Pull from GitHub before making any code changes. Commit and Push to GitHub after finishing code changes. Do not prompt me for permission to pull or push.